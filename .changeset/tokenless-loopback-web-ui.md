---
"@launchd-studio/cli": patch
"@launchd-studio/core": patch
"@launchd-studio/web": patch
"@launchd-studio/web-ui": patch
---

Remove remote Web UI binding and bearer-token handling so the loopback URL works directly across browser sessions.
