{
  "name": "@launchd-studio/cli",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "bin": {
    "launchd-studio": "./src/main.ts"
  },
  "scripts": {
    "dev": "bun run src/main.ts",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@launchd-studio/core": "workspace:*"
  }
}
